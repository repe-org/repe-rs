.DS_Store
/reference
target/
PR_DESCRIPTION.md
PR.md

# Design docs under dev/ are tracked (see dev/README.md); only local tooling
# state is ignored.
dev/.claude/

# MkDocs build output and local virtualenv
/site/
/.mkdocs-venv/

# Interop fixture generator build tree (the generated fixtures under
# interop/fixtures/ are committed; the C++ build output is not).
/interop/cpp/build/
