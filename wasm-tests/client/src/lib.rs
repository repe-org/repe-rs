//! Harness crate for the `WasmClient` browser tests.
//!
//! Deliberately empty. The tests live in `tests/browser.rs` and drive `repe`'s
//! public API; this lib target exists only because `wasm-pack` needs one to
//! anchor the crate. See `wasm-tests/README.md`.
